using UnityEngine;

public class Themes : MonoBehaviour {
    
    private Theme[] themes = {
        new Theme() {
            Name = "Default",
            BackgroundColor = Color.black
        }
    };
    
    
}